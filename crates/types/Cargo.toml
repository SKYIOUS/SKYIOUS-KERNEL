[package]
name = "vahi-types"
version.workspace = true
edition.workspace = true
description = "Shared types and trait interfaces that break circular dependencies between kernel modules"

[dependencies]
spin = "0.9"

[features]
default = []
