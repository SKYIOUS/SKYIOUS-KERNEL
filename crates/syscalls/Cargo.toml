[package]
name = "vahi-syscalls"
version = "0.3.0"
edition = "2021"
description = "System call dispatch and implementations"

[dependencies]
vahi-sync = { path = "../sync" }
vahi-types = { path = "../types" }
spin = "0.9.8"

[features]
default = []
self_test = []
