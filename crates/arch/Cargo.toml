[package]
name = "vahi-arch"
version = "0.3.0"
edition = "2021"
description = "Architecture-specific code for the Vahi kernel (x86_64, aarch64)"

[dependencies]
vahi-sync = { path = "../sync" }
spin = "0.9.8"

